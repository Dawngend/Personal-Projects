.venv/
.streamlit/
__pycache__/
reviewer.db
uploads/
course_brain_db/
extraction_cache/
.staging/
apps/web/*.tsbuildinfo
deploy/staging/.env.staging
deploy/staging/cloudflared/config.yml
deploy/staging/cloudflared/*.json
deploy/staging/secrets/

# Debug dump written when a provider response will not parse. It can contain
# raw model output, so it must never be committed.
groq_raw_error.txt
